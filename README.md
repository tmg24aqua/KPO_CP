# 📘 Компилятор языка ENV-2025

> **Курсовой проект по дисциплине "Конструирование программного обеспечения"**

Проект представляет собой разработку полнофункционального транслятора для учебного языка программирования **ENV-2025**. Компилятор выполняет лексический, синтаксический и семантический анализ, преобразует выражения в обратную польскую запись и генерирует код на языке ассемблера (MASM) для платформы x86.

## 🛠 Технологический стек

*   **Язык реализации:** C++ (ISO C++14 Standard)
*   **Среда разработки:** Visual Studio 2022
*   **Целевая платформа:** Windows 11 (64-bit)
*   **Выходной код:** Assembly (MASM)

## 📋 Спецификация языка

### Типы данных
*   **Integer (`integer`):** Знаковое целое число (2 байта / 16 бит). Диапазон: `[-32,768 ... 32,767]`.
*   **Symbol (`symbol`):** Символьный тип данных.
*   **String (`string`):** Строковый тип (используется для операций вывода и сравнения).

### Литералы
*   **Целочисленные:**
    *   Десятичные: `123`, `-5`
    *   Шестнадцатеричные: `'0x1A'`, `'-0xFF'`
    *   Двоичные: `'0b101'`, `'-0b11'`
*   **Логические:** Гибкое представление (`true`/`false`, `yes`/`no`, `on`/`off`, `1`/`0`).

### Операторы и конструкции
*   **Арифметика:** `+`, `-`, `*`, `/` (деление нацело), `%` (остаток от деления).
*   **Ввод/Вывод:** Операторы `write` и `writeline` для вывода данных в стандартный поток.
*   **Циклы:** Оператор цикла с фиксированным числом повторений:
    ```text
    repeat 5 times do [ ... ]
    ```

### Стандартная библиотека
В язык встроены следующие функции:
*   `compare(s1, s2)` — лексикографическое сравнение строк.
*   `lenght(s)` — возвращает длину строки.
*   `random(min, max)` — генерация случайного числа в заданном диапазоне.
*   `pow(base, exp)` — возведение числа в степень.

## 🏗 Архитектура компилятора

Проект построен по модульному принципу и включает следующие этапы:

1.  **Lexer (Лексический анализатор):** Разбиение исходного кода на токены, работа с таблицами лексем и идентификаторов.
2.  **Parser (Синтаксический анализатор):** Анализ на основе грамматики Грейбах с использованием автомата с магазинной памятью (MFST).
3.  **Semantic Analyzer (Семантический анализатор):** Проверка соответствия типов, контроль областей видимости и валидация параметров функций.
4.  **Polish Notation (Польская нотация):** Преобразование арифметических и логических выражений в ПОЛИЗ (RPN) для удобства вычислений.
5.  **Generator (Генератор кода):** Трансляция промежуточного представления в код Ассемблера (`.asm`).

## 🚀 Пример кода (ENV-2025)

Ниже приведен пример программы, вычисляющей факториал числа:

```text
integer function factorial(integer n)
{
    type integer result = 1;
    type integer i = 1;
    repeat n times do
    [
        result = result * i;
        i = i + 1;
    ]
    return result;
}

main
{
    type integer num = 5;
    type integer fact = factorial(num);
    write "Factorial of 5 is: ";
    writeline fact;
}
